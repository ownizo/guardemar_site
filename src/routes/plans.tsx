import { createFileRoute } from '@tanstack/react-router'
import { Check } from 'lucide-react'
import { AssessmentCta, Breadcrumbs, ButtonLink, FAQSection, PageHero } from '@/components/site'
import { plans } from '@/config/site'

export const Route=createFileRoute('/plans')({head:()=>({meta:[{title:'Property Care Plans and Pricing | Guardemar'},{name:'description',content:'Transparent monthly property care plans from €69 for second homes and holiday homes in the Western Algarve.'}],links:[{rel:'canonical',href:'https://guardemar.com/plans/'}]}),component:PlansPage})
function PlansPage(){return <><Breadcrumbs items={[{label:'Home',to:'/'},{label:'Plans'}]}/><PageHero eyebrow="Care plans" title="A level of oversight suited to your property." text="Choose a starting plan, then confirm the right scope through a first property assessment. Pricing reflects the home, access, systems and agreed tasks." aside="Clear plans. Clear exclusions. Property-specific recommendations."/><section className="section warm-section"><div className="shell plans-grid detailed-plans">{plans.map(plan=><article className={`plan-card ${plan.popular?'popular':''}`} key={plan.name}>{plan.popular&&<span className="popular-label">Most popular</span>}<p className="eyebrow">{plan.name}</p><div className="price">From <strong>€{plan.price}</strong><span>/month</span></div><p>{plan.description}</p><ul>{plan.items.map(item=><li key={item}><Check size={15}/>{item}</li>)}</ul><ButtonLink to="/contact/" event="plan_cta_click">Request an assessment</ButtonLink></article>)}</div><div className="shell plan-clarity"><p>Pool maintenance, gardening and professional repairs are not included, but can be coordinated. No plan promises 24/7 emergency response.</p><p>Prices shown are indicative and may be subject to VAT and property-specific assessment. Custom plans are available for larger villas, estates and properties with complex requirements.</p></div></section><FAQSection limit={7}/><AssessmentCta/></>}
